/**
 * 움직이는 아이콘 컴포넌트를 구현해봅시다!
 * 요구사항 _ prop으로 아이콘 이미지 파일(로컬)과, 스타일 속성을 전달 받을 거예요!
 * 애니메이션은 모두 동일하게, 커졌다 작아졌다하는 귀여운 모션으로 제작해볼까요?
 */
const Icon = ({ icon, style }) => {
  return <></>;
};

export default Icon;
